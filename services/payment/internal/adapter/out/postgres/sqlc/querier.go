// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1

package sqlc

import (
	"context"
	"time"

	"github.com/google/uuid"
)

type Querier interface {
	// Takes the key for this attempt, or reports that somebody already has it.
	//
	// ON CONFLICT DO NOTHING rather than an upsert: a second claim must not
	// overwrite the first one's request_hash, which is what tells a retry from a
	// key reused for different content. A caller that gets no row back reads the
	// existing claim and decides what to answer.
	//
	// Run inside the transaction that writes the attempt. A concurrent submit
	// blocks here until that transaction ends, and then either finds the committed
	// claim or takes the key the rollback released.
	ClaimIdempotencyKey(ctx context.Context, arg ClaimIdempotencyKeyParams) (IdempotencyKey, error)
	CreatePayment(ctx context.Context, arg CreatePaymentParams) (Payment, error)
	// The sweep. Kept 24h: long enough to outlive any retry a client is still
	// making, short enough that the table stays small.
	DeleteExpiredIdempotencyKeys(ctx context.Context, createdAt time.Time) (int64, error)
	GetIdempotencyKey(ctx context.Context, arg GetIdempotencyKeyParams) (IdempotencyKey, error)
	GetPayment(ctx context.Context, id uuid.UUID) (Payment, error)
	// Settling reads the attempt and then writes it, and the two writers that meet
	// here are ordinary rather than rare: the provider's callback can arrive before
	// the response to the call that caused it. FOR UPDATE makes Postgres order
	// them, so the second reads the first one's outcome instead of racing it to a
	// version number and losing.
	GetPaymentForUpdate(ctx context.Context, id uuid.UUID) (Payment, error)
	// Every attempt against a batch of orders in one round trip. Looping the
	// single-order query per row is the N+1 this exists to remove.
	GetPaymentsByOrderIDs(ctx context.Context, orderIds []uuid.UUID) ([]Payment, error)
	// The optimistic lock is the WHERE clause: a writer carrying a stale version
	// affects zero rows and finds out, rather than overwriting whatever the other
	// one decided. It is a backstop behind GetPaymentForUpdate rather than the
	// main guard, and it costs nothing to keep.
	SettlePayment(ctx context.Context, arg SettlePaymentParams) (Payment, error)
}

var _ Querier = (*Queries)(nil)
