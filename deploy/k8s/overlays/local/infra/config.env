# The gateway's per-cluster values on a laptop.

# The storefront's dev server. Kong is reached on localhost:8000 through the
# k3d load balancer, so a browser calling this API is on localhost too.
CORS_ORIGINS=["http://localhost:3000"]
